node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vercel
.vinxi
__unconfig*
todos.json

# pipeline runtime artifacts — ship only the two runtime JSON shapes the app
# reads (index.json + each creator's dataset.json, bundled via import.meta.glob);
# keep raw media/transcripts/frames/cookies out of git.
data/creators/*
!data/creators/index.json
!data/creators/*/
data/creators/*/*
!data/creators/*/dataset.json
# Yahoo quoteType cache (symbol-scope.ts). Regenerable + static per symbol, so a
# local/VM cache, never committed; survives the VM's data/prices-scoped git clean.
data/symbol-types.json
.chrome-profile
playwright-report

# og image previews (local)
.og-preview/

# build-time precompute (scripts/prebuild.ts) — regenerated on every build
public/og/
public/og.png
public/datasets/
public/prices/
public/avatars/
public/llms.txt
public/calls-index.json

# bake-off generated reports
bakeoff-glm5p2-*.md
