# Implementation Plans

Generated by the `improve` skill on 2026-06-11, audited against commit `fa39041`.
Each plan is self-contained: an executor with zero context from the audit session
can run it from the plan file + the repo alone. Read the plan fully before
starting, honor its STOP conditions, and update your status row when done.

Verification gates used by every plan: `bunx tsc --noEmit` and `bun test`
(tests import `bun:test`; DB-backed suites skip unless `DATABASE_URL_TEST` etc.
are set). The `#/` import alias maps to `src/`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | CI runs `bun test` + `tsc` on every PR | P1 | S | — | DONE — merged to local `main` |
| 002  | Validate LLM classification; stop silent X drops | P1 | M | 001 | DONE — merged to local `main` |
| 003  | `forwardReturn` → null (not fake 0%) on uncovered series | P1 | S | 001 | DONE — merged to local `main` |
| 004  | One first-call per ticker on same-day ties | P2 | S | 001 | DONE — merged to local `main` |
| 005  | Skip reels with no `upload_date` (no fabricated "today") | P2 | S | 001, 002 | DONE — merged to local `main` |
| 006  | `prices.ts`: filter null OHLC + refetch under-covered cache | P2 | S | 001 | DONE — merged to local `main` |
| 007  | Crash-safe X extract checkpoint (no duplicate calls) | P2 | S | 001, 002 | DONE — merged to local `main` |
| 008  | Validate serve-seam input (params, `firstDate`, cache cap, token) | P3 | S | 001 | DONE — merged to local `main` |
| 009  | Post-merge holistic-review fixes (CI mock leak, prices restatement, extract catch, chart-fetch validation) | P1 | M | 001–008 | DONE — merged + pushed to `origin/main` (`6029f1f`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Integration state (2026-06-12)

All 8 plans executed (each by a fresh-context executor in an isolated worktree),
reviewed, and **merged into local `main`** (tip `5497e15`). Verification on the
combined tree: `bunx tsc --noEmit` exit 0; `bun test` → 137 pass / 20 skip / 0 fail.
Agent worktrees and `advisor/*` branches removed. **Local `main` is ahead of
`origin/main` and not yet pushed** (pushing auto-deploys to Vercel prod — held for
explicit confirmation).

Two follow-ups that are NOT plan TODOs (outside the executor/advisor scope):
- **Re-score** affected creators (002/003/004/005/006 change *future* scored output;
  the committed frozen datasets are unchanged). Operator step: re-run `pipeline`/
  `pipeline:x` → `score` for each creator, then `bun run scripts/parity-check.ts`
  (must print `PARITY OK`). Needs API keys + network — cannot run in CI/advisor.
- **Stale comment** at `pipeline/x/extract-x.ts` (names `gpt-oss-120b`/`qwen3p6-plus`;
  CLAUDE.md says superseded by `deepseek-v4-flash`/`kimi-k2p5`) — pre-existing, trivial.

## Recommended sequence

1. **001 first** — it's the verification baseline. Every other plan's "Done
   criteria" lean on `bun test` running; CI should enforce it before re-scored
   data or refactors are trusted.
2. **Scoring-correctness cluster (002, 003, 004, 006)** — independent of each
   other; can be done in any order or in parallel worktrees. All four change
   *future* scored output, so each carries the same maintenance note: a re-score
   + `bun run scripts/parity-check.ts` (must print `PARITY OK`) is a separate
   **operator** step, not part of the plan. None regenerate committed data.
3. **005 after 002**, and **007 after 002** — both edit files that 002 also
   touches (`pipeline/extract.ts` and `pipeline/x/extract-x.ts` respectively).
   Sequencing avoids a merge conflict; each notes the expected post-002 shape.
4. **008 any time after 001** — isolated to the serve seams; no data impact.

## Dependency notes

- 002 → 005: both edit the `pipeline/extract.ts` classify loop.
- 002 → 007: both edit `pipeline/x/extract-x.ts` (002 changes `tweetToReelCall`
  internals; 007 changes load/push/persist — different regions, but sequence to
  avoid conflicts).
- 003 and 006 are complementary: 006 stops under-covered price caches from
  reaching scoring; 003 is the correctness backstop if one slips through.

## Re-scoring caveat (applies to 002, 003, 004, 005, 006)

These fix logic, not committed data. The frozen datasets
(`data/creators/*/dataset.json`, `data/prices/*.json`) were produced by the old
code and are **not** regenerated by any plan (no API keys / network in CI). After
a plan lands, an operator re-running the pipeline (`pipeline` / `pipeline:x` →
`score`) for affected creators, then `bun run scripts/parity-check.ts` →
`PARITY OK`, applies the correction. Expect published hit-rate / avgExcess numbers
to shift slightly once re-scored.

## Findings considered and rejected (so they aren't re-audited)

- **Remove `vitest` (claimed unused devDep)** — false: imported by
  `pipeline/retry.test.ts` and `pipeline/vision.test.ts`. (Real but minor: two
  files use `vitest` while the rest and CLAUDE.md use `bun:test` — a runner
  inconsistency, not dead code. Not worth a plan now.)
- **Remove `shadcn` (claimed unused dep)** — false: `src/styles.css:9` imports
  `shadcn/tailwind.css`.
- **`parity-check.ts` missing / no gate** — false: `scripts/parity-check.ts`
  exists and is thorough; it's a documented manual cutover gate.
- **`closeOnOrAfter` → binary search (claimed render hot path)** — rejected: it
  runs at build-time scoring, baked into `dataset.json`; not a per-render path.
  Negligible at current scale (2 creators); revisit only if scoring time becomes
  a measured bottleneck.
- **Frames assume 60s video (`pipeline/frames.ts:24`)** — real (short reels lose
  vision hints), but IG-only, build-time, and not selected this round. Worth a
  small follow-up: read duration from the yt-dlp `.info.json` and seek to real
  percentages.
- **`"latest"`-pinned `@tanstack/*` deps (reproducibility)** — real, not selected
  this round. Pin to the versions in `bun.lock` when convenient.

## Direction options (not planned — for the maintainer to weigh)

- **D1 — Wire Plan 3b CDN purge.** `src/routes/api/revalidate.ts` `purge()` is a
  logged no-op; the "live data, no redeploy" goal stalls at the 6h ISR TTL until
  the Vercel-API purge call is wired (needs `VERCEL_API_TOKEN` on the VM). A
  design/spike plan, not in this set.
- **D2 — imos-vm cron ingestion + ops runbook.** Pipeline is fully manual; 2
  creators onboarded vs the spec's 10–50. The VM is named as the ingest host but
  has no cron, dead-man-switch, or runbook.
- **D3 — TikTok adapter.** CLAUDE.md claims a new platform "only needs to resolve
  its URL + call `saveAvatar`"; worth a spike to validate against TikTok's
  bot-detection before committing.
