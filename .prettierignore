# oxfmt honors this file. Keep committed generated/data artifacts out of `fmt`.

# Pipeline output committed as build source — reformatting churns 100k+ lines and
# must byte-match the pipeline's own JSON output (see CLAUDE.md "Datasets").
data/

# Generated by the TanStack Router plugin, regenerated on every build/dev run —
# don't fight the generator (its output isn't oxfmt-formatted).
src/routeTree.gen.ts
